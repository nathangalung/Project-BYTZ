pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,tsx,jsx,json}"
      run: ./node_modules/.bin/biome check --no-errors-on-unmatched --staged {staged_files}
      stage_fixed: true
